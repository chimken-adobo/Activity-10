# ============================================
# Event Registration Backend - Environment Variables
# ============================================
# Copy this file to .env and update the values for your environment
# ============================================

# --------------------------------------------
# Server Configuration
# --------------------------------------------
# Port number for the backend API server
PORT=3000

# Environment mode: 'development' | 'production'
# - In development: database auto-sync is enabled, logging is enabled
# - In production: database auto-sync is disabled, logging is disabled
NODE_ENV=development

# --------------------------------------------
# Database Configuration (SQLite)
# --------------------------------------------
# Optional: Specify custom path to SQLite database file
# Defaults to database.sqlite at project root if not specified
# Example: DB_PATH=/path/to/database.sqlite
DB_PATH=database.sqlite

# --------------------------------------------
# JWT Authentication Configuration
# --------------------------------------------
# Secret key used to sign JWT tokens
# IMPORTANT: Change this to a strong, random secret in production!
JWT_SECRET=your-secret-key-change-this

# JWT token expiration time
# Format: number + unit (e.g., '7d' for 7 days, '24h' for 24 hours, '1h' for 1 hour)
JWT_EXPIRES_IN=7d

# --------------------------------------------
# SMTP/Email Configuration
# --------------------------------------------
# SMTP server hostname
# For Gmail, use: smtp.gmail.com
# For Outlook, use: smtp-mail.outlook.com
SMTP_HOST=smtp.gmail.com

# SMTP server port
# Common ports: 587 (TLS), 465 (SSL), 25 (unencrypted)
SMTP_PORT=587

# SMTP authentication username (usually your email address)
SMTP_USER=your-email@gmail.com

# SMTP authentication password
# For Gmail: Use an App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password

# Default "from" email address for outgoing emails
# This will appear as the sender in ticket confirmations and announcements
SMTP_FROM=noreply@events.com
